export default function main(){
  //**************************************************************************
  // EVENTS *this binds the receiving object (eg. onClick - this is the button clicked)
  return '<button onclick="this.innerHTML = Date()">The time is?</button>'
  
}